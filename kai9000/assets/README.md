# Lum avatar drop point

Place the production Lum model here as `avatar.glb`.

Until that asset exists, the APK uses a teal procedural capsule so the 3D scene, camera, UI, Ollama probe, and Android export can be validated independently.
